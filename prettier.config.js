// Prettier is currently not installed, this config exists so that prettier-based formatters (such as the VS Code Svelte extension)
// stick to these formatting rules
export default {
	useTabs: true,
	singleAttributePerLine: false,
	printWidth: 120,
};
